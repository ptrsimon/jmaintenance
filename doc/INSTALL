Installation instructions - jmaintenance

To install jmaintenance, run the installer script and choose Install or Upgrade respectively.

It will install jmaintenance to /opt/jmaintenance and create a symbolic link to /usr/local/sbin.